package org.example.MainCode.SQLIntepreter;

public enum TokenType{
    SELECT,
    FROM,
    WHERE,
    IDENTIFIER,
    COMMA,
    ALL,
    NUMBER,
    STRING,
    OPERATOR,
    LPAREN,
    RPAREN,
    ALIAS,
    DOT,
    JOIN,
    EOF
}
